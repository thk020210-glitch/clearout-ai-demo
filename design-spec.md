# 设计说明 — 闲鱼 · AI 自主清售计划

**生成日期：** 2026-08-09
**源 PRD：** 闲鱼_AI自主清售计划_PRD_v3.docx（v3.0）

## 概览

六屏高保真移动端原型，覆盖 PRD §8.1 中列出的全部必要界面。其中「价格计划」与「报价处理」两屏为完整可交互 Demo：所有数值均由 PRD §5、§6 中的公式在浏览器中实时计算得出，而非预先截图或写死的状态。

## 界面清单

| 屏幕 | 文件 | 对应 PRD 章节 | 交互深度 |
|---|---|---|---|
| 发布成功入口 | `entry.html` | §8.1-1 | 静态高保真 |
| 多件商品概览 | `dashboard.html` | §8.1-2, §2.2 | 静态高保真（可点击进入吉他详情） |
| 单品设置 | `item-setup.html` | §8.1-3, §4.1 | 半交互（滑杆/开关实时更新展示值，保存后传递参数） |
| 价格计划 | `price-plan.html` | §8.1-4, §5 | **完整可交互** —公式实时计算 |
| 报价处理 | `offer-engine.html` | §8.1-5, §6 | **完整可交互** —公式实时计算 |
| 计划结果 | `results.html` | §8.1-6, §10 | 静态高保真 |

## 公式实现对照（§5 / §6）

价格计划页与报价处理页中的每一个数字都对应 PRD 中的一条公式，未做简化替换：

- `P0 = round(clamp(M + a_s·R, F, M_high))`
- `k = clamp(k_l + k_s, 0.65, 2.40)`（k_l 固定取「中」=1.60，与 §5.2 一致，因流动性由 AI 判断、用户不可直接选择）
- `P_base(t) = F + (P0−F)·(r/T)^k`
- `Q = clamp(0.30·收藏强度 + 0.35·有效报价强度 − 0.20·供给压力 − 0.15·低价报价密度, −1, 1)`
- `P_target = clamp(P_base(t) + 0.08·R·Q, F, P_current)`
- `P_next = max(F, max(P_current·(1−降幅上限), P_target))`
- `κ(t) = clamp(κ_strategy + κ_urgency, 0.05, 0.45)`，`L(t) = round(max(0.85F, F − κ·(P(t)−F)))`
- `γ(t)` 按剩余天数分段 + 策略修正，`C(t) = round(clamp(F + γ·(P(t)−F), F, P(t)))`
- 四分支判定：`B<L(t)` 自动拒绝 / `L(t)≤B<F` AI 还价 / `F≤B<P(t)` 有效报价 / `B≥P(t)` 优先确认

**验证：** 以 PRD §8.2 默认 Demo 场景（吉他，F=¥450，M∈[520,760]，平衡出售，r=14/T=14）代入，原型独立计算得出 P0=¥688、C(t)=¥486 —— 与 PRD §6.3 文案示例中的「若提升至 ¥486」完全一致，四个演示报价（¥350/¥430/¥560/当前价）均落入 PRD 规定的对应分支。

**简化说明：** 「有效报价强度」与「低价报价密度」两个信号在 Demo 中默认收纳于「高级信号」折叠面板（PRD §8.3 明确要求可控的是剩余天数/策略/底价/收藏强度/供给压力/买家报价，这两项未被列为必须暴露的控件，故设为默认可调但不常驻展示）。P_target 的 P_current 基准在本 Demo 中取当前 P_base(t) 快照，用于单次「假设推演」，而非跨天序列状态机——这与「系统只保持或下调公开价」的原则一致，但不模拟多日连续调价历史。

## 设计方向

**视觉语言：** 本产品定位为嵌入闲鱼「自己卖」路径、与无忧卖并列的原生功能（PRD §0），而非独立 App。因此设计没有走「大胆实验」的路线，而是贴合闲鱼真实的移动端视觉语言：暖橙/暖金主色、浅灰背景、白色圆角卡片、PingFang SC 系统字体——这是为了让熟悉闲鱼产品的评审者（面试官）一眼认出「这是闲鱼里长出来的功能」，而不是一个自说自话的新皮肤。

**信号元素：** 价格曲线图表（价格计划页）是本原型的核心视觉锚点——用一条从 P0 平滑衰减到 F 的曲线，把 PRD 中最核心的产品判断（时间×策略×流动性驱动的价格衰减模型）变成可以「拖动看见」的东西。这也直接呼应 PRD §7 的「每次自动动作必须带有可读解释」原则：曲线旁边永远跟着一段用真实计算结果生成的解释文案。

**色彩：** 主色 #FF4B10（价格、CTA）与 #FFC300（品牌强调色）取自闲鱼的暖色系；候选交易用绿色 #0EA972 表示「需要你确认的好消息」，自动拒绝用中性灰表示「系统已处理，无需打扰」，一次还价用金色表示「等待中」——三种状态色对应 PRD §7 状态表中三种不同的「是否打扰卖家」语义。

**信息克制：** 买家可见与卖家可见的字段被严格区分（PRD §6.4）——报价处理页顶部的「当前情境卡」明确标注 F 底价「仅你可见」，卡片下方的援引文案也不透露底价或曲线，与 PRD 的信息边界要求保持一致。

## 技术说明

- 纯静态 HTML/CSS/JS，无构建依赖，双击 `index.html` 或任意单页文件即可在浏览器中打开。
- 每个页面均为单文件自包含（内联 CSS 与 JS），可独立分享、独立打开。
- 页面间通过 URL query string 传递关键状态（例如 `item-setup.html` 保存后携带 `F`、`strategy` 跳转至 `price-plan.html`；后者又携带 `F/strategy/r/P` 跳转至 `offer-engine.html`），已做端到端点击验证。
- 已在 Chromium 无头浏览器中逐屏截图 + 控制台报错检查，六个页面均无 JS 报错。
- 移动端视口宽度 430px 设计，横向居中展示，适配桌面浏览器预览与真机预览两种场景。

## 后续可迭代方向

- 「多件商品概览」当前仅吉他一件商品可进入完整交互（对应 PRD §8.2「交互深度以吉他为主案例验证」），其余三件商品为状态展示；后续可扩展为同一套引擎的多实例。
- 「候选交易」卡片的接受/拒绝目前是前端状态切换，未连接真实的「卖家确认」二次弹窗与「取消后释放」超时逻辑（PRD §6.3 状态机的完整实现）。
- 价格曲线目前展示的是「快照式」单次推演，如需展示 PRD §5.3 中「按检查节点顺序调价、降幅逐步累加」的完整多日轨迹，需要引入时间序列状态（比当前的纯函数式计算复杂一档）。
